/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  13
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    format      ascii;
    class       dictionary;
    location    "system";
    object      controlDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

// The film patch types (filmWall, mappedFilmSurface, ...) are registered by
// libisothermalFilm. extrudeToRegionMesh creates the film region boundary with
// those types but does not link the film solver module, so on Windows (PE/COFF,
// no load-time global-namespace symbol resolution) the type is not registered
// and extrusion fails with "Unknown polyPatch type filmWall". Declare the
// dependency here so argList loads it for every utility in the case; harmless on
// Linux where the library is already available.
libs            ("libisothermalFilm.so");

regionSolvers
{
    film            film;
    panel           solid;
    box             fluid;
}

startFrom       startTime;

startTime       0;

stopAt          endTime;

endTime         5;

deltaT          1e-4;

writeControl    adjustableRunTime;

writeInterval   0.1;

purgeWrite      0;

writeFormat     binary;

writePrecision  10;

writeCompression off;

timeFormat      general;

timePrecision   6;

runTimeModifiable yes;

adjustTimeStep  yes;

maxCo           0.2;

maxDeltaT       5e-3;

// ************************************************************************* //
