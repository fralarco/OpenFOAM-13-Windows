EXE_INC = \
    -I$(FOAM_SRC)/parallel/decompose/decompositionMethods/lnInclude \
    -I$(FOAM_SRC)/parallel/decompose/metis/lnInclude

LIB_LIBS =

ifeq ($(WM_ARCH),mingw_w64)
    # PE/COFF requires explicit link closure for directly
    # referenced symbols; ELF resolves them at load time
    LIB_LIBS += -ldecompositionMethods
endif
